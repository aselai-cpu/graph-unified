# GraphRAG Unified - Environment Variables
# Copy this file to .env and fill in your API keys

# ============================================
# LLM API Keys (Required)
# ============================================

# Anthropic API key for Claude (for entity/relationship extraction)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI API key (alternative LLM provider - optional)
# OPENAI_API_KEY=sk-your-openai-key-here

# Azure OpenAI (alternative LLM provider - optional)
# AZURE_OPENAI_API_KEY=your-azure-key-here
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# ============================================
# Embedding API Keys (Optional)
# ============================================

# Voyage AI API key (for embeddings)
# Get your key from: https://www.voyageai.com/
# NOTE: Not needed if using local embeddings (see settings-local-embeddings.yaml)
VOYAGE_API_KEY=pa-your-voyage-key-here

# OpenAI API key for embeddings (alternative - optional)
# OPENAI_API_KEY=sk-your-openai-key-here

# Cohere API key for embeddings (alternative - optional)
# COHERE_API_KEY=your-cohere-key-here

# ============================================
# Local Embeddings (FREE Alternative)
# ============================================
# If you want to use FREE local embeddings instead of paid APIs:
# 1. Use settings-local-embeddings.yaml as your config
# 2. Install: pip install sentence-transformers torch
# 3. No API key needed! See LOCAL_EMBEDDINGS_GUIDE.md

# ============================================
# Storage Configuration (Optional)
# ============================================

# Output directory for indexed data
# STORAGE_ROOT_DIR=./output

# Vector database backend
# VECTOR_DB_BACKEND=lancedb

# ============================================
# Performance Tuning (Optional)
# ============================================

# Number of concurrent workers
# WORKERS=4

# Enable profiling for performance debugging
# ENABLE_PROFILING=false

# ============================================
# Logging Configuration (Optional)
# ============================================

# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# Log format: text, json
# LOG_FORMAT=text

# Log output: stdout, file, both
# LOG_OUTPUT=stdout

# ============================================
# Notes
# ============================================
#
# 1. Never commit .env file to git (it's in .gitignore)
# 2. Use ${VAR_NAME} syntax in YAML config files to reference these variables
# 3. Example in settings.yaml:
#    llm:
#      api_key: ${ANTHROPIC_API_KEY}
#      provider: anthropic
#
# 4. For local embeddings (no API key needed), see:
#    - LOCAL_EMBEDDINGS_GUIDE.md
#    - settings-local-embeddings.yaml
#
# 5. The system automatically searches for .env in:
#    - Same directory as config file
#    - Current working directory
#    - Parent directories (up to 3 levels)
